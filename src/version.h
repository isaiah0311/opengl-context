/**
 * \file version.h
 * \author Isaiah Lateer
 * 
 * Header file for the version struct.
 */

#ifndef OPENGL_CONTEXT_VERSION_HEADER
#define OPENGL_CONTEXT_VERSION_HEADER

typedef struct version {
    int major, minor;
} version;

#endif
